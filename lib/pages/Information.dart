import 'package:flutter/material.dart';

class Information extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Information", style: TextStyle(fontWeight: FontWeight.w900, fontSize: 20),),
    );
  }
}