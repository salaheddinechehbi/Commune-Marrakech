import 'package:flutter/material.dart';

class Reclamation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Relamation", style: TextStyle(fontWeight: FontWeight.w900, fontSize: 20),),
    );
  }
}